import styled from "@emotion/styled";

// Source
import { device } from "@theme";

export const ServiceAare = styled.section`
    padding: 65px 0 0;
    position: relative;
    z-index: 1;
    ${device.small} {
        padding: 135px 0 0;
    }
`;
